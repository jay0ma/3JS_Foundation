{
  "name": "core-vs-rim",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Interactive 3D driving piece: chaos on the rim, structure at the core.",
  "scripts": {
    "dev": "vite --host 0.0.0.0 --port 5173",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0 --port 4173"
  },
  "dependencies": {
    "cannon-es": "^0.20.0",
    "three": "^0.160.0"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  }
}
